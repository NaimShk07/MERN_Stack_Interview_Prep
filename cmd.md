# 🧭 Terminal Essentials: Navigate & Work with Files

## 📁 Folder Navigation

- `pwd` → Show current folder (Print Working Directory)
- `ls` → List contents

  - `ls -a` → Show hidden files

- `cd foldername` → Go into folder
  - `cd ..` → Go back (up one level)
  - `cd` → Go to home
  - `cd -` → Previous folder

## 🛣️ Paths You Need

- `.` → Current folder
- `..` → Parent folder
- `~` → Home directory
- `/` → Root directory

## 📄 File & Folder Commands

- `mkdir folder` → Create new folder
- `rm -r folder` → Delete folder & contents ⚠️
- `touch file.txt` → Create empty file
- `rm file.txt` → Delete file

## 🧠 Pro Tips

- **Tab** → Auto-complete folder/file names
- **Ctrl + L** → Clear screen
- `history` → Show past commands
- `!n` → Rerun command #n from history

---
