1. import statements are hoisted to the top of the file before any other code runs.
2. module scope
3. Math.random() \* (max - min) + min
4. event.clientX - size(height/width) / 2 : clicked point on web
